# Production requirements - minimal runtime dependencies
# For Railway deployment: Start with just API packages, add vector store only if needed

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
requests==2.31.0
pyyaml==6.0.1

# LLM client
google-genai==0.2.2

# Vector store dependencies - OPTIONAL (only needed if vector store is enabled)
faiss-cpu==1.12.0
numpy>=1.25.0,<3.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
sentence-transformers==2.2.2
transformers==4.35.0