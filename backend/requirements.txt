# Production requirements - minimal runtime dependencies
# For Railway deployment: No torch, no sentence-transformers
# Vector store uses Google's embedding API for queries

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.28.1

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
requests==2.31.0
pyyaml==6.0.1

# LLM client
google-genai==1.37.0

# Vector store - FAISS works with numpy arrays directly
# No torch needed - embeddings come from Google's API
faiss-cpu==1.12.0
numpy>=1.25.0,<3.0
