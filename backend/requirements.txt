# Production requirements
# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.28.1

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
requests==2.31.0
pyyaml==6.0.1

# LLM client
google-genai==1.37.0

# Vector store - requires sentence-transformers for query embeddings
# Default: CPU-only (lightweight, ~150MB for torch)
# If GPU detected, requirements-gpu.txt will override with GPU versions
faiss-cpu==1.12.0
numpy>=1.25.0,<2.0
sentence-transformers==2.2.2
--no-deps transformers==4.35.0
# Install CPU-only torch from PyTorch's wheel index (no CUDA dependencies)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
