# Requirements for building vector index locally
# Install with: pip install -r requirements-build-index.txt
# Only needed when creating/rebuilding the vector index

# Include production requirements
-r requirements.txt

numpy>=1.25.0,<2.0
sentence-transformers==2.2.2
torch==2.1.0
transformers==4.35.0
