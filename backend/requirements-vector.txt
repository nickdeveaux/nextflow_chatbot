# Optional dependencies for vector store implementation
# Install with: pip install -r requirements-vector.txt

# Vector store (FAISS)
faiss-cpu==1.7.4  # Use faiss-gpu if you have CUDA
numpy==1.24.3  # Required by FAISS

# Embeddings (sentence-transformers)
sentence-transformers==2.2.2
torch==2.1.0  # Required by sentence-transformers
transformers==4.35.0

# Alternative: Use OpenAI embeddings instead (already in requirements.txt)
# openai==1.3.0

