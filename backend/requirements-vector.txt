# Vector store dependencies - install separately if vector search is needed
# Install with: pip install -r requirements-vector.txt
# Or add to requirements.txt if vector store is required

# FAISS for vector search (lightweight, no GPU needed)
faiss-cpu==1.12.0
numpy>=1.25.0,<3.0
torch==2.1.0
sentence-transformers==2.2.2
transformers==4.35.0

