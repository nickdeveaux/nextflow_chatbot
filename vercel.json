{
  "buildCommand": "cd frontend && npm install 2>&1 | grep -v 'npm warn deprecated' || true && npm run build",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs",
  "rootDirectory": "frontend",
  "installCommand": "cd frontend && npm install 2>&1 | grep -v 'npm warn deprecated' || true",
  "devCommand": "cd frontend && npm run dev"
}

